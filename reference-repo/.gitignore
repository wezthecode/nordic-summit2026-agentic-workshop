# .NET build output
bin/
obj/
*.user

# User secrets are stored outside the repo (~/.microsoft/usersecrets/<id>/secrets.json) —
# never committed, but excluding these paths too in case anyone copies one in by hand.
secrets.json
**/secrets.json

# OS/editor cruft
.DS_Store
.vs/
.vscode/
*.swp

# Local scratch/log output from workshop runs
*.log
